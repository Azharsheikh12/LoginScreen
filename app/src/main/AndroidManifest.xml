<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginscreen">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="stateAlwaysHidden|adjustResize"
        android:theme="@style/Theme.LoginScreen">
        <activity android:name=".GetStarted">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Circle" />
        <activity android:name=".Scroll_Piechart" />
        <activity android:name=".InstructorProfile" />
        <activity android:name=".CourseGridActivity" />
        <activity android:name=".CardLayout" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".VolleyListActivity" />
        <activity android:name=".BottomNavActivity" />
    </application>

</manifest>